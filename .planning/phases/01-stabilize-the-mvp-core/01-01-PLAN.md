---
phase: 01-stabilize-the-mvp-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/page.tsx
  - app/globals.css
  - app/components/feed-surface.tsx
  - app/components/profile-surface.tsx
  - app/components/tracking-surface.tsx
  - app/components/topbar-shell.tsx
  - app/components/ui/status-chip.tsx
autonomous: true
requirements:
  - DISC-01
  - DISC-02
  - DISC-03
  - PROF-01
  - PROF-02
  - PROF-03
user_setup: []
must_haves:
  truths:
    - The main page delegates feed, profile, and tracking rendering to dedicated surface components instead of keeping all product UI in one monolithic component.
    - The refreshed Profile surface preserves profile basics, resume upload state, and preference editing without regressing existing save flows.
    - The refreshed Tracking surface can still be mounted from the main page without breaking feed browsing or refresh behavior.
  artifacts:
    - app/components/feed-surface.tsx
    - app/components/profile-surface.tsx
    - app/components/tracking-surface.tsx
    - app/components/topbar-shell.tsx
  key_links:
    - app/page.tsx imports and renders FeedSurface, ProfileSurface, and TrackingSurface.
    - app/globals.css contains Phase 1 surface classes that implement the approved spacing, typography, and color contract from 01-UI-SPEC.md.
---

<objective>
Split the current product surface into stable responsibility-based UI modules and apply the Phase 1 UI refresh to tracking/profile without changing the product architecture.

Purpose: Reduce regression risk in `app/page.tsx`, preserve demo stability, and make future tracking/profile work tractable.
Output: Dedicated surface components, a lighter page shell, and CSS tokens/classes aligned with the approved UI contract.
</objective>

<execution_context>
@/home/oatar/.codex/get-shit-done/workflows/execute-plan.md
@/home/oatar/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-stabilize-the-mvp-core/01-CONTEXT.md
@.planning/phases/01-stabilize-the-mvp-core/01-RESEARCH.md
@.planning/phases/01-stabilize-the-mvp-core/01-UI-SPEC.md
@app/page.tsx
@app/globals.css
@app/api/profile/route.ts
@app/api/automation-runs/route.ts
@app/api/automation-ready-jobs/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract responsibility-based page surfaces</name>
  <files>app/page.tsx, app/components/feed-surface.tsx, app/components/profile-surface.tsx, app/components/tracking-surface.tsx, app/components/topbar-shell.tsx</files>
  <read_first>app/page.tsx, app/globals.css, .planning/phases/01-stabilize-the-mvp-core/01-CONTEXT.md, .planning/phases/01-stabilize-the-mvp-core/01-UI-SPEC.md, app/api/profile/route.ts, app/api/automation-runs/route.ts</read_first>
  <action>Create `FeedSurface`, `ProfileSurface`, `TrackingSurface`, and `TopbarShell` under `app/components/`. Move the existing render logic out of `app/page.tsx` into these components without changing the current data flow or page-level state ownership yet. Keep `app/page.tsx` as the orchestration shell that owns fetch/mutation state and passes explicit props down. Do not create a new global state layer. Use exact exported component names `FeedSurface`, `ProfileSurface`, `TrackingSurface`, and `TopbarShell` so later plans can target them directly.</action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - `app/components/feed-surface.tsx` contains `export function FeedSurface(`
    - `app/components/profile-surface.tsx` contains `export function ProfileSurface(`
    - `app/components/tracking-surface.tsx` contains `export function TrackingSurface(`
    - `app/components/topbar-shell.tsx` contains `export function TopbarShell(`
    - `app/page.tsx` contains `import { FeedSurface } from "@/app/components/feed-surface"`
    - `app/page.tsx` contains `import { ProfileSurface } from "@/app/components/profile-surface"`
    - `app/page.tsx` contains `import { TrackingSurface } from "@/app/components/tracking-surface"`
  </acceptance_criteria>
  <done>The product page renders through extracted surfaces with no type errors and no new state container.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the approved Phase 1 tracking/profile UI contract</name>
  <files>app/globals.css, app/components/profile-surface.tsx, app/components/tracking-surface.tsx, app/components/ui/status-chip.tsx</files>
  <read_first>app/globals.css, .planning/phases/01-stabilize-the-mvp-core/01-UI-SPEC.md, app/page.tsx</read_first>
  <action>Add the Phase 1 visual contract directly to the extracted tracking/profile surfaces. Create a reusable `StatusChip` component at `app/components/ui/status-chip.tsx` and use it for normalized run states. Update CSS so the extracted surfaces follow the explicit contract from `01-UI-SPEC.md`: 16px body text, 14px bold labels, 20px headings, 44px display only where already justified, standard spacing tokens `4/8/16/24/32/48/64`, accent `#155EEF` reserved for primary CTA, active tab, focus ring, selected state, and key status emphasis only. Keep the atmospheric light palette; do not introduce a new design system or dark theme.</action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - `app/components/ui/status-chip.tsx` contains `export function StatusChip(`
    - `app/components/profile-surface.tsx` contains `Save profile changes`
    - `app/components/tracking-surface.tsx` contains `No automation runs yet`
    - `app/globals.css` contains `.profile-surface`
    - `app/globals.css` contains `.tracking-surface`
    - `app/globals.css` contains `.status-chip`
    - `app/globals.css` contains `#155EEF`
  </acceptance_criteria>
  <done>Tracking and profile surfaces visibly follow the approved UI contract and expose normalized status copy through a shared status chip.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve feed filtering and refresh behavior through the new shell</name>
  <files>app/page.tsx, app/components/feed-surface.tsx</files>
  <read_first>app/page.tsx, app/components/feed-surface.tsx, app/api/automation-ready-jobs/route.ts, .planning/REQUIREMENTS.md</read_first>
  <action>Keep feed browsing, supported-source/ATS scoping, and job refresh behavior intact after the surface extraction. If feed filter state is currently implicit in `app/page.tsx`, make it explicit and pass it into `FeedSurface` as props rather than reading from nested UI logic. Preserve the current refresh path and ensure extraction does not reset profile/tracking state unexpectedly when the feed refreshes.</action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - `app/components/feed-surface.tsx` contains `export function FeedSurface(`
    - `app/page.tsx` contains `<FeedSurface`
    - `app/page.tsx` contains `startTransition(`
    - `app/page.tsx` contains `refresh`
  </acceptance_criteria>
  <done>The feed still supports ranked browsing and refresh behavior through the new shell without profile/tracking regressions.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit`
- [ ] Manual profile save and resume upload still work in the extracted UI
- [ ] Manual feed refresh still works and does not wipe profile/tracking state
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `app/page.tsx` is reduced to an orchestration shell instead of rendering all three product surfaces inline
- Profile and tracking surfaces visibly match the Phase 1 UI contract while preserving existing behavior
</success_criteria>

<output>
After completion, create `.planning/phases/01-stabilize-the-mvp-core/01-01-SUMMARY.md`
</output>
