---
phase: 01-stabilize-the-mvp-core
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - tools/stagehand-runner/server.mjs
  - .gitignore
  - docs/automation-execution.md
  - n8n/job-apply-stagehand-orchestrator.workflow.json
  - n8n/job-apply-anchor-orchestrator.workflow.json
  - n8n/job-apply-ai-agent.workflow.json
  - scripts/automation-worker.js
autonomous: true
requirements:
  - DISC-03
  - TRAK-03
user_setup: []
must_haves:
  truths:
    - The repo makes the active browser-execution path obvious and stops implying that legacy automation experiments are still supported.
    - Stagehand runner temp/browser-profile data no longer pollutes the repo worktree during normal use.
    - Application records and automation-run outcomes remain separate concepts in the documented executor contract and active orchestrator path.
  artifacts:
    - docs/automation-execution.md
    - n8n/job-apply-stagehand-orchestrator.workflow.json
  key_links:
    - docs/automation-execution.md explicitly labels Stagehand as active and Browser Use as experimental.
    - tools/stagehand-runner/server.mjs writes transient browser state outside the repo tree or otherwise prevents in-repo profile sprawl.
    - Obsolete workflow or worker artifacts are removed or clearly marked as deprecated, not silently left as apparent options.
---

<objective>
Remove dead-path automation clutter and define a clear active/experimental executor contract around the current Stagehand path.

Purpose: Make the repo engineering-clean enough for Phase 2 automation work and investor-demo preparation.
Output: One explicit automation execution document, a cleaned automation file set, and Stagehand runtime hygiene that stops polluting the worktree.
</objective>

<execution_context>
@/home/oatar/.codex/get-shit-done/workflows/execute-plan.md
@/home/oatar/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-stabilize-the-mvp-core/01-CONTEXT.md
@.planning/phases/01-stabilize-the-mvp-core/01-RESEARCH.md
@.planning/codebase/CONCERNS.md
@tools/stagehand-runner/server.mjs
@lib/automation-orchestrator.ts
@lib/automation.ts
@n8n/job-apply-stagehand-orchestrator.workflow.json
@scripts/automation-worker.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document and label the supported executor path</name>
  <files>docs/automation-execution.md, n8n/job-apply-stagehand-orchestrator.workflow.json</files>
  <read_first>.planning/phases/01-stabilize-the-mvp-core/01-CONTEXT.md, .planning/phases/01-stabilize-the-mvp-core/01-RESEARCH.md, lib/automation-orchestrator.ts, n8n/job-apply-stagehand-orchestrator.workflow.json</read_first>
  <action>Create `docs/automation-execution.md` as the single Phase 1 source of truth for browser execution. The document must explicitly define three labels: `active`, `experimental`, and `deprecated`. Mark `Stagehand LOCAL + Playwright` as `active`. Mark `Browser Use` as `experimental`. Mark legacy managed-browser experiments and abandoned `n8n` agent paths as `deprecated`. Document that the app is the source of truth, the orchestrator is transport, and the executor returns normalized run outcomes and evidence.</action>
  <verify>rg -n "active|experimental|deprecated|Stagehand LOCAL \+ Playwright|Browser Use" docs/automation-execution.md</verify>
  <acceptance_criteria>
    - `docs/automation-execution.md` contains `Stagehand LOCAL + Playwright`
    - `docs/automation-execution.md` contains `active`
    - `docs/automation-execution.md` contains `Browser Use`
    - `docs/automation-execution.md` contains `experimental`
    - `docs/automation-execution.md` contains `deprecated`
  </acceptance_criteria>
  <done>The supported executor path is documented clearly enough that engineers and demo operators can tell what is real and what is legacy.</done>
</task>

<task type="auto">
  <name>Task 2: Delete obsolete automation paths and stop ambiguous repo state</name>
  <files>n8n/job-apply-anchor-orchestrator.workflow.json, n8n/job-apply-ai-agent.workflow.json, scripts/automation-worker.js</files>
  <read_first>.planning/codebase/STRUCTURE.md, .planning/codebase/CONCERNS.md, docs/automation-execution.md, scripts/automation-worker.js</read_first>
  <action>Delete clearly obsolete automation artifacts that no longer represent the active Phase 1 path. At minimum, remove the legacy Anchor orchestrator JSON, the legacy AI-agent workflow JSON that no longer matches the current execution model, and the old `scripts/automation-worker.js` worker if it is no longer part of the supported runtime. Do not delete the Stagehand orchestrator. If any artifact must remain for reference, move that reference into `docs/automation-execution.md` instead of leaving the executable file in place as a false option.</action>
  <verify>test ! -f n8n/job-apply-anchor-orchestrator.workflow.json && test ! -f n8n/job-apply-ai-agent.workflow.json && test ! -f scripts/automation-worker.js</verify>
  <acceptance_criteria>
    - `n8n/job-apply-anchor-orchestrator.workflow.json` does not exist
    - `n8n/job-apply-ai-agent.workflow.json` does not exist
    - `scripts/automation-worker.js` does not exist
  </acceptance_criteria>
  <done>The repo no longer presents dead automation paths as live choices.</done>
</task>

<task type="auto">
  <name>Task 3: Fix Stagehand runtime hygiene and worktree pollution</name>
  <files>tools/stagehand-runner/server.mjs, .gitignore</files>
  <read_first>tools/stagehand-runner/server.mjs, .gitignore, .planning/codebase/CONCERNS.md</read_first>
  <action>Change the Stagehand runner so temporary browser/profile state is created outside the repo tree using OS temp directories or another non-repo path. Remove any logic that writes Stagehand Chromium profiles or scratch inspection outputs under `tools/stagehand-runner/`. Update `.gitignore` only for residual unavoidable local artifacts, not as a substitute for the runtime fix. The goal is that normal Stagehand runs no longer create new tracked-or-untracked browser profile directories under the repo.</action>
  <verify>rg -n "mkdtemp|os\.tmpdir|tmpdir" tools/stagehand-runner/server.mjs && rg -n "stagehand-v3|profile-" .gitignore</verify>
  <acceptance_criteria>
    - `tools/stagehand-runner/server.mjs` contains `os.tmpdir()` or `mkdtemp(` for transient browser state
    - `tools/stagehand-runner/server.mjs` does not contain a repo-relative temporary profile path under `tools/stagehand-runner`
    - `.gitignore` contains `tools/stagehand-runner/*.tmp` or another explicit narrow ignore for unavoidable runner scratch files if still needed
  </acceptance_criteria>
  <done>Normal Stagehand execution no longer pollutes the repo with browser profile artifacts.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit`
- [ ] `npm run build`
- [ ] Fresh Stagehand runner activity does not create new browser profile directories under the repo tree
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repo has one obvious active automation path and no dead-path ambiguity
- Stagehand runtime artifacts stop polluting the worktree during normal development
</success_criteria>

<output>
After completion, create `.planning/phases/01-stabilize-the-mvp-core/01-03-SUMMARY.md`
</output>
