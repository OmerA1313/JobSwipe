---
phase: 01-stabilize-the-mvp-core
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - lib/automation.ts
  - lib/automation-orchestrator.ts
  - lib/automation-debug.ts
  - app/api/automation-runs/[id]/route.ts
  - app/components/tracking-surface.tsx
  - package.json
  - vitest.config.ts
  - tests/automation.serialization.test.ts
autonomous: true
requirements:
  - TRAK-01
  - TRAK-02
  - TRAK-03
user_setup: []
must_haves:
  truths:
    - Automation run details are normalized in one shared serialization path instead of duplicating provider-specific extraction logic in two libraries.
    - Tracking surfaces display status, blocker, screenshot evidence, and action trace before raw payloads.
    - The repo has a minimal automated test harness protecting automation serialization and manual-attention classification.
  artifacts:
    - lib/automation-debug.ts
    - tests/automation.serialization.test.ts
    - vitest.config.ts
  key_links:
    - lib/automation.ts and lib/automation-orchestrator.ts both import shared debug/serialization helpers from lib/automation-debug.ts.
    - app/components/tracking-surface.tsx renders the normalized fields returned by app/api/automation-runs/[id]/route.ts in the order defined by 01-UI-SPEC.md.
    - package.json exposes a stable test command that Phase 1 execution can run repeatedly.
---

<objective>
Harden automation evidence serialization and the tracking experience so blocked runs are understandable, debuggable, and protected by minimal regression tests.

Purpose: Make tracking trustworthy enough for repeated demos and future automation work.
Output: Shared debug serializers, clearer tracking evidence ordering, and a first Phase 1 test harness.
</objective>

<execution_context>
@/home/oatar/.codex/get-shit-done/workflows/execute-plan.md
@/home/oatar/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-stabilize-the-mvp-core/01-CONTEXT.md
@.planning/phases/01-stabilize-the-mvp-core/01-RESEARCH.md
@.planning/phases/01-stabilize-the-mvp-core/01-VALIDATION.md
@.planning/phases/01-stabilize-the-mvp-core/01-UI-SPEC.md
@lib/automation.ts
@lib/automation-orchestrator.ts
@app/api/automation-runs/[id]/route.ts
@app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract shared automation debug serializers</name>
  <files>lib/automation.ts, lib/automation-orchestrator.ts, lib/automation-debug.ts</files>
  <read_first>lib/automation.ts, lib/automation-orchestrator.ts, .planning/phases/01-stabilize-the-mvp-core/01-RESEARCH.md, .planning/phases/01-stabilize-the-mvp-core/01-VALIDATION.md</read_first>
  <action>Create `lib/automation-debug.ts` and move the duplicated payload parsing, URL normalization, Anchor/Browserbase/Stagehand debug extraction, and manual-attention classification into shared functions there. Update both `lib/automation.ts` and `lib/automation-orchestrator.ts` to import those helpers instead of carrying local copies. Keep the app-visible debug shape stable: `debug.anchor`, `debug.browserbase`, `debug.stagehand`, normalized `latestEvent`, and `requiresManualAttention` must still exist.</action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - `lib/automation-debug.ts` contains `export function extractStagehandDebug(`
    - `lib/automation-debug.ts` contains `export function extractBrowserbaseDebug(`
    - `lib/automation-debug.ts` contains `export function extractAnchorDebug(`
    - `lib/automation.ts` contains `from "@/lib/automation-debug"`
    - `lib/automation-orchestrator.ts` contains `from "@/lib/automation-debug"`
  </acceptance_criteria>
  <done>All executor debug parsing and manual-attention classification live in one shared library.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild tracking detail hierarchy around normalized evidence</name>
  <files>app/api/automation-runs/[id]/route.ts, app/components/tracking-surface.tsx</files>
  <read_first>app/api/automation-runs/[id]/route.ts, app/components/tracking-surface.tsx, .planning/phases/01-stabilize-the-mvp-core/01-UI-SPEC.md, .planning/phases/01-stabilize-the-mvp-core/01-CONTEXT.md</read_first>
  <action>Make the run detail endpoint and Tracking surface enforce the Phase 1 evidence hierarchy: normalized status/current step first, blocker explanation second, screenshot evidence third, action trace fourth, raw payloads last. Add an explicit `Manual attention` presentation path distinct from `Needs input` and `Failed`. Keep raw payloads visible by default, but move them below the screenshot and action trace so they do not dominate the surface.</action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - `app/components/tracking-surface.tsx` contains `Manual attention`
    - `app/components/tracking-surface.tsx` contains `Stagehand snapshot`
    - `app/components/tracking-surface.tsx` contains `Action trace`
    - `app/components/tracking-surface.tsx` contains `Latest Stagehand payload`
    - `app/api/automation-runs/[id]/route.ts` contains `requiresManualAttention`
  </acceptance_criteria>
  <done>Blocked runs read clearly from summary to evidence to raw payloads, with manual-attention boundaries explicitly distinguished.</done>
</task>

<task type="auto">
  <name>Task 3: Install the Phase 1 validation harness</name>
  <files>package.json, vitest.config.ts, tests/automation.serialization.test.ts</files>
  <read_first>package.json, .planning/phases/01-stabilize-the-mvp-core/01-VALIDATION.md, lib/automation.ts, lib/automation-orchestrator.ts</read_first>
  <action>Add `vitest` as the Phase 1 unit-test framework, create `vitest.config.ts` if required, and add `tests/automation.serialization.test.ts` covering shared debug extraction, manual-attention classification, and stable run-debug shape. Add a package script named `test:automation` that runs the focused Phase 1 tests. Keep the test target narrow; do not introduce browser E2E infrastructure in this phase.</action>
  <verify>npx vitest run tests/automation.serialization.test.ts && npx tsc --noEmit</verify>
  <acceptance_criteria>
    - `package.json` contains `"test:automation"`
    - `vitest.config.ts` contains `defineConfig`
    - `tests/automation.serialization.test.ts` contains `describe(`
    - `tests/automation.serialization.test.ts` contains `requiresManualAttention`
  </acceptance_criteria>
  <done>Phase 1 has a repeatable automated safety net for automation serialization and manual-attention logic.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run tests/automation.serialization.test.ts`
- [ ] `npx tsc --noEmit`
- [ ] A blocked run in the UI shows summary, blocker, screenshot, and action trace before raw payloads
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Tracking/debug logic is normalized through a shared library instead of duplicate serializers
- The UI makes blocked-run evidence legible without hiding raw data
</success_criteria>

<output>
After completion, create `.planning/phases/01-stabilize-the-mvp-core/01-02-SUMMARY.md`
</output>
